name: ecommerce_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  #Nav bar do Google
  google_nav_bar: ^5.0.6
  #Provider para autalização automática de dados
  provider: 
  #Biblioteca para funcionar com Back4App
  parse_server_sdk_flutter: ^9.0.0
  #Gerenciador de estados
  get: ^4.6.6
  http: ^0.13.6
  #Images
  image_picker: ^1.0.4
  image_picker_for_web: ^3.0.1
  #Outros
  validatorless: ^1.2.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/
    - .env